# Packaging metadata lives in pyproject.toml. This file exists only for flake8,
# which still does not read pyproject.toml.
[flake8]
max-line-length = 100
extend-ignore = E203, W503
exclude = .git,__pycache__,.venv,build,dist,*.egg-info,dashboard/node_modules
